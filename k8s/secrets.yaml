# ============================================================================
# DocIntel AI Platform - Secrets
# ============================================================================
# IMPORTANT: Replace ALL placeholder values with your own base64-encoded secrets.
#
# To encode a value:
#   echo -n "your-secret-value" | base64
#
# SECURITY NOTES:
#   - NEVER commit real secrets to version control
#   - Use a secrets manager (Vault, AWS Secrets Manager, etc.) in production
#   - Consider using SealedSecrets or External Secrets Operator for GitOps
#   - This file is a TEMPLATE only
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: docintel-secrets
  namespace: docintel
  labels:
    app.kubernetes.io/name: docintel-secrets
    app.kubernetes.io/part-of: docintel-platform
type: Opaque
data:
  # PostgreSQL connection string
  # Replace with base64-encoded value: echo -n "postgresql://user:pass@host:5432/docintel" | base64
  DATABASE_URL: "cG9zdGdyZXNxbDovL3VzZXI6cGFzc0Bob3N0OjU0MzIvZG9jaW50ZWw="

  # JWT signing key - MUST be changed for production
  # Replace with base64-encoded value: echo -n "your-jwt-secret-key" | base64
  JWT_SECRET_KEY: "Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24="

  # Google Gemini API key for AI-powered document analysis
  # Replace with base64-encoded value: echo -n "your-gemini-api-key" | base64
  GEMINI_API_KEY: "Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24="

  # RabbitMQ credentials
  # Replace with base64-encoded value: echo -n "amqp://user:pass@rabbitmq:5672/" | base64
  RABBITMQ_URL: "YW1xcDovL2d1ZXN0Omd1ZXN0QHJhYmJpdG1xOjU2NzIv"

  # Redis password (if authentication is enabled)
  # Replace with base64-encoded value: echo -n "your-redis-password" | base64
  REDIS_PASSWORD: ""
